# Launching Haskell 

- Open terminal & type in `ghci` to run ghc's interactive mode

- cmd + d to exit

- __Boolean Algebra__
  - && ➡️ and
  - || ➡️ or
  - not ➡️ negates `True` or `False`

- __Equality__
  - `/=` ➡️ 'is not equal'
    - `5 /= 5` returns `False`
  - `==` ➡️ equality